{
  "name": "adr-generation-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "n8n end-to-end workflow: meeting recording → ADR with LangChain nodes, HITL review and Confluence/Jira/Slack publish",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:workflow": "node scripts/build-workflow.js"
  }
}
